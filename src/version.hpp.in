#pragma once

#define MKSTR(x) #x
#define CHIP8_MKSTR(x) MKSTR(x)

#define CHIP8_VERSION_MAJOR ${PROJECT_VERSION_MAJOR}
#define CHIP8_VERSION_MINOR ${PROJECT_VERSION_MINOR}
#define CHIP8_VERSION_PATCH ${PROJECT_VERSION_PATCH}

#define CHIP8_VERSION_NAME "CHIP-8 Emulator"
#define CHIP8_VERSION_NUMBER CHIP8_MKSTR(CHIP8_VERSION_MAJOR.CHIP8_VERSION_MINOR.CHIP8_VERSION_PATCH)
#define CHIP8_VERSION "v" CHIP8_VERSION_NUMBER
